rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Diário escolar pessoal — acesso livre (site privado do professor)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
